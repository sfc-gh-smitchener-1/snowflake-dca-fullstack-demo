fastapi>=0.104.0
uvicorn[standard]>=0.24.0
snowflake-snowpark-python>=1.11.0
pydantic>=2.5.0
httpx>=0.25.0

# Neo4j driver — only required when GRAPH_BACKEND includes 'neo4j' or 'both'.
# The Snowflake-native backend has no extra runtime deps. The driver is kept
# in the default image so operators can switch backends without rebuilding.
neo4j>=5.14.0
